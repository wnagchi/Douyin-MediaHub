# 未分类资源兼容 - 实施总结与验证指南

## 功能概览

本次实现让系统能够兼容非标准命名的图片和视频文件，不再跳过它们。这些文件会被标记为"未分类"并在主列表中单独展示。

## 已完成的修改

### 1. 后端 - media/index.js
- ✅ 导出 `extToKind()` 函数供 indexer 复用

### 2. 后端 - indexer.js

#### 2.1 新增辅助函数
- ✅ `formatMtimeToTimeText(mtimeMs)` - 将文件 mtime 转换为展示用时间格式
- ✅ `buildFallbackRecord(filename, ext, mtimeMs, dirId)` - 为非标准命名文件构建数据记录
  - 仅处理图片和视频（kind='image'/'video'）
  - 使用文件修改时间作为时间戳
  - 文件名（去扩展名）作为主题
  - 标记 `isUnclassified: true`

#### 2.2 数据库迁移
- ✅ 添加 `isUnclassified INTEGER DEFAULT 0` 字段
- ✅ 创建索引 `idx_items_unclassified`
- ✅ 老数据自动回填 `isUnclassified = 0`

#### 2.3 扫描逻辑增强
- ✅ `parseMediaFilename` 失败时不再直接跳过
- ✅ 检查文件扩展名，如果是图片/视频则使用 fallback 解析
- ✅ `upsert` 时写入 `isUnclassified` 字段

#### 2.4 查询分组优化
- ✅ 修改 `queryResources` 的分组键逻辑：
  - 标准资源：`timeText|author|theme`
  - 未分类资源：`dirId|DATE(timeText)` 避免碎片化
- ✅ 返回 group 带 `isUnclassified` 标识
- ✅ 未分类组的展示字段：
  - `author`: "未分类"
  - `theme`: "未分类 · YYYY-MM-DD"
  - `themeText`: "未分类 · YYYY-MM-DD"

### 3. 前端 - api.ts
- ✅ `MediaGroup` 接口添加 `isUnclassified?: boolean` 字段

### 4. 前端 - App.tsx
- ✅ 将 `state.groups` 拆分为 `standardGroups` 和 `unclassifiedGroups`
- ✅ 分别计算 `visibleItems` 和 `unclassifiedVisibleItems`
- ✅ 修正 `groupIdx` 映射（未分类组索引 = 标准组数量 + 偏移）
- ✅ 两种视图模式都支持未分类分区：
  - **瀑布流模式**：标准内容后显示"未分类资源"标题 + 独立瀑布流
  - **合集模式**：标准内容后显示"未分类资源"标题 + 独立合集列表

## 功能特性

### 资源识别规则
```
标准命名格式：
  "YYYY-MM-DD HH.MM.SS-类型-发布人-主题(_序号).ext"
  示例：2025-12-07 16.29.19-视频-张三-旅行日记_1.mp4

非标准命名（兼容）：
  任意名称的图片/视频文件
  示例：IMG_1234.jpg, video.mp4, 我的照片.png
```

### 未分类资源特征
- **来源时间**：使用文件系统修改时间（mtime）
- **展示标题**：`未分类 · YYYY-MM-DD`
- **作者**：显示为"未分类"
- **主题**：原始文件名（去扩展名）
- **类型**：标记为"未分类"
- **分组规则**：按目录 + 日期分组，避免每个文件单独成组

### 展示位置
- 在主列表**底部**固定展示（不依赖筛选）
- 有明显的分隔线和标题区分
- 显示组数统计

## 验证步骤

### 测试 1：非标准命名文件扫描

#### 准备测试文件
```bash
# 在配置的 media 目录下创建测试文件
cd /path/to/media

# 创建非标准命名的图片
echo "test" > photo.jpg
echo "test" > IMG_1234.png
echo "test" > 我的照片.webp

# 创建非标准命名的视频
echo "test" > video.mp4
echo "test" > 电影.mov
```

#### 执行扫描
1. 访问前端页面
2. 点击"同步内容"按钮
3. 等待扫描完成

#### 预期结果
- 控制台无错误
- 数据库中插入了这些文件记录
- `isUnclassified = 1`

#### 验证数据库
```bash
sqlite3 data/index.sqlite "SELECT filename, isUnclassified, typeText, author, theme FROM media_items WHERE isUnclassified=1;"
```

**预期输出：**
```
photo.jpg|1|未分类||photo
IMG_1234.png|1|未分类||IMG_1234
我的照片.webp|1|未分类||我的照片
video.mp4|1|未分类||video
电影.mov|1|未分类||电影
```

### 测试 2：前端未分类分区显示

#### 访问主页
```
http://localhost:3000/
```

#### 预期界面
- 页面正常加载
- 标准资源在上方正常显示
- 底部出现分隔线
- 显示标题："未分类资源 (X 组)"
- 未分类资源按日期分组展示

#### 切换视图模式
1. 点击"瀑布流/合集"切换按钮
2. 两种模式都应该显示未分类分区

### 测试 3：预览功能

#### 操作步骤
1. 点击未分类分区中的某个图片/视频缩略图
2. 预览模态框应正常打开
3. 左右切换应正常工作
4. 关闭预览

#### 预期结果
- 预览正常显示
- 导航功能正常
- 无控制台错误

### 测试 4：批量操作

#### 操作步骤
1. 点击"多选"按钮进入选择模式
2. 选择一些未分类资源
3. 点击"下载"或"删除"

#### 预期结果
- 批量下载：未分类资源也被包含在 zip 中
- 批量删除：未分类资源被成功删除
- 操作后刷新，数据正确更新

### 测试 5：混合场景

#### 准备环境
- 同一目录下既有标准命名文件，也有非标准命名文件

#### 预期结果
- 标准命名文件在标准分区正常分组
- 非标准命名文件在未分类分区按日期分组
- 两个分区互不干扰

### 测试 6：类型筛选

#### 操作步骤
1. 点击顶部"视频"筛选
2. 观察未分类分区

#### 预期结果
- 标准分区只显示视频
- 未分类分区中的视频仍然显示
- 未分类分区中的图片被过滤掉

### 测试 7：目录筛选

#### 操作步骤
1. 在顶部选择特定目录
2. 观察未分类分区

#### 预期结果
- 标准分区只显示该目录的资源
- 未分类分区只显示该目录的未分类资源

## 数据结构示例

### 标准资源记录
```sql
dirId: "abc123"
filename: "2025-12-07 16.29.19-视频-张三-旅行日记_1.mp4"
timeText: "2025-12-07 16.29.19"
author: "张三"
theme: "旅行日记"
typeText: "视频"
isUnclassified: 0
```

### 未分类资源记录
```sql
dirId: "abc123"
filename: "my_photo.jpg"
timeText: "2025-12-15 10.23.45" (from mtime)
author: ""
theme: "my_photo"
typeText: "未分类"
isUnclassified: 1
```

## 前端展示示例

```
┌─────────────────────────────────┐
│ 标准资源分区                      │
│ [视频卡片] [图集卡片] [实况卡片]    │
│ ...                              │
├─────────────────────────────────┤
│ 未分类资源 (3 组)                 │
│ 未分类 · 2025-12-15              │
│   [photo.jpg] [video.mp4]        │
│ 未分类 · 2025-12-14              │
│   [IMG_1234.png]                 │
└─────────────────────────────────┘
```

## API 响应变化

### GET /api/resources
返回的 groups 数组中，未分类组包含额外字段：

```json
{
  "groups": [
    {
      "id": "...",
      "author": "未分类",
      "theme": "未分类 · 2025-12-15",
      "themeText": "未分类 · 2025-12-15",
      "groupType": "未知",
      "isUnclassified": true,
      "items": [...]
    }
  ]
}
```

## 性能影响

- **扫描性能**：每个文件多一次 `extToKind` 检查，影响可忽略
- **数据库大小**：新增一个 INTEGER 字段和索引，影响极小
- **查询性能**：分组键略复杂（CASE WHEN），但有索引支持
- **前端渲染**：分区拆分纯内存操作，无明显影响

## 已知限制

1. **非媒体文件仍被跳过**：txt、zip、doc 等不会入库
2. **无元数据推断**：无法从文件内容提取 EXIF 时间或作者信息
3. **分组粒度**：未分类资源按天分组，可能一天一个组包含多个文件
4. **搜索能力有限**：未分类资源只能按文件名搜索，无作者/主题等元数据

## 后续增强建议

1. **EXIF 提取**：对图片读取 EXIF 时间和地理位置
2. **智能分组**：对未分类资源基于文件名模式智能分组
3. **批量重命名**：提供 UI 工具批量修正为标准命名
4. **类型推断**：基于文件内容或缩略图智能识别类型

## 回滚方案

如需回滚：
1. 恢复 `indexer.js` 中的 `if (!p) continue;` 逻辑
2. 前端恢复原 `visibleItems` 和 sections 计算
3. 数据库中的 `isUnclassified` 字段可保留（不影响旧逻辑）

## 兼容性说明

- ✅ 向后兼容：老数据自动回填 `isUnclassified=0`
- ✅ 前端向下兼容：未分类字段为空时不显示分区
- ✅ 环境变量优先级不变
- ✅ 所有现有功能（预览、删除、下载、筛选）继续工作
